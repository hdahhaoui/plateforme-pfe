// Stub très simple pour éviter les erreurs sur Vercel.
// Tu pourras le remplacer plus tard par ta logique de matching réelle.

export async function recomputeAssignments(pb) {
  console.log('[recomputeAssignments] Stub exécuté sur Vercel (aucun calcul effectué).');
  // Ici on pourrait plus tard :
  // - lire les choix (collection "choices")
  // - lire les sujets (collection "subjects")
  // - calculer les affectations
  // - mettre à jour "currentAssignment" etc.
}
